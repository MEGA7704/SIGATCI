import {api,esc,fmtDate,showToast,professionalConfirm,professionalAlert,withButtonLock} from './common.js';
async function load(){try{const d=await api('/api/users');const tb=document.getElementById('usersBody');tb.innerHTML=d.items.map(u=>`<tr><td>${esc(u.display_name)}</td><td>${esc(u.username)}</td><td>${esc(u.email||'—')}</td><td><span class="pill">${esc(u.role_code)}</span></td><td>${esc(u.status)}</td><td>${fmtDate(u.last_login_at)}</td><td><div class="actions"><button class="btn btn-secondary btn-sm" data-reset="${u.id}">Réinitialiser mot de passe</button><button class="btn btn-secondary btn-sm" data-toggle="${u.id}" data-status="${u.status==='ACTIVE'?'DISABLED':'ACTIVE'}">${u.status==='ACTIVE'?'Désactiver':'Activer'}</button><button class="btn btn-danger btn-sm" data-delete="${u.id}">Supprimer</button></div></td></tr>`).join('')||'<tr><td colspan="7">Aucun utilisateur.</td></tr>';d.items.forEach(u=>{tb.querySelector(`[data-reset="${u.id}"]`)?.addEventListener('click',()=>resetPwd(u.id));tb.querySelector(`[data-toggle="${u.id}"]`)?.addEventListener('click',e=>setStatus(u.id,e.currentTarget.dataset.status));tb.querySelector(`[data-delete="${u.id}"]`)?.addEventListener('click',()=>delUser(u.id))});const r=await api('/api/password-requests');document.getElementById('requestsBox').innerHTML=r.items.filter(x=>x.status==='PENDING').map(x=>`<div class="notice" style="margin-bottom:8px"><strong>${esc(x.display_name||x.username_or_email)}</strong> — ${fmtDate(x.requested_at)} — demande en attente</div>`).join('')||'Aucune demande en attente.'}catch(e){document.getElementById('userMsg').innerHTML=`<div class="notice error">${esc(e.message)}</div>`;document.getElementById('addUserBtn').disabled=true}}
async function resetPwd(id){const yes=await professionalConfirm('Réinitialiser le mot de passe','Un mot de passe temporaire sera généré pour cet utilisateur.',{confirmText:'Réinitialiser'});if(!yes)return;try{const r=await api('/api/users/reset-password',{method:'POST',body:{userId:id}});await professionalAlert('Mot de passe temporaire',`Mot de passe temporaire (affiché une seule fois) :\n\n${r.temporaryPassword}`,{copyText:r.temporaryPassword});load()}catch(e){await professionalAlert('Réinitialisation impossible',e.message)}}
async function setStatus(id,status){try{await api('/api/users/status',{method:'POST',body:{userId:id,status}});showToast('Statut mis à jour.');load()}catch(e){showToast(e.message,'error')}}
async function delUser(id){const yes=await professionalConfirm('Archiver l’utilisateur','L’utilisateur sera désactivé et archivé. Ses historiques seront conservés.',{confirmText:'Archiver'});if(!yes)return;try{await api('/api/users/delete',{method:'POST',body:{userId:id}});await professionalAlert('Utilisateur archivé','Le compte utilisateur a été archivé.');load()}catch(e){await professionalAlert('Archivage impossible',e.message)}}
document.addEventListener('DOMContentLoaded',()=>{const d=document.getElementById('userDialog');document.getElementById('addUserBtn').onclick=()=>d.showModal();document.querySelectorAll('[data-close-user]').forEach(b=>b.onclick=()=>d.close());document.getElementById('userForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/users/create',{method:'POST',body:{displayName:document.getElementById('uName').value,username:document.getElementById('uUsername').value,email:document.getElementById('uEmail').value,phone:document.getElementById('uPhone').value,role:document.getElementById('uRole').value,password:document.getElementById('uPassword').value}});d.close();e.target.reset();showToast('Utilisateur créé.');load()}catch(err){showToast(err.message,'error')}};load()});
